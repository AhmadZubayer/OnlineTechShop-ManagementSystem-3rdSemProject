CREATE OR REPLACE VIEW CartDetails AS
SELECT p.PRODUCT_NAME, c.QUANTITY, p.PRICE, p.VAT, 
       ROUND(c.QUANTITY * p.PRICE, 3) AS TOTAL_PRICE, 
       ROUND(c.QUANTITY * p.PRICE * p.VAT / 100, 3) AS VAT_AMOUNT,
       c.C_USERNAME 
FROM cart c, products p   
WHERE c.PRODUCT_ID = p.PRODUCT_ID;  

